# Wikipedia Grade A++ Translator Dependencies
# Minimal requirements - standard library is used for SQLite, urllib, regex, etc.
# rich is optional for rich UI, requests is supported as alternative HTTP client.

requests>=2.28.0
rich>=13.0.0
